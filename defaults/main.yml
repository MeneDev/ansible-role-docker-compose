---
docker_compose_version: latest
docker_compose_url: "{{ docker_compose[ansible_system][docker_compose_version]['url'] }}"
docker_compose_checksum: "{{ docker_compose[ansible_system][docker_compose_version]['checksum'] }}"

# empty is the default path for your os
#docker_compose_path: ""
